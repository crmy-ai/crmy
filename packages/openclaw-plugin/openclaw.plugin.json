{
  "id": "crmy",
  "name": "CRMy — Customer Context Engine",
  "configSchema": {
    "type": "object",
    "properties": {
      "serverUrl": {
        "type": "string",
        "description": "CRMy server URL. Defaults to .crmy.json, ~/.crmy/config.json, auth state, then http://localhost:3000."
      },
      "apiKey": {
        "type": "string",
        "description": "CRMy API key or login token. Defaults to CRMY_API_KEY, .crmy.json, ~/.crmy/config.json, then ~/.crmy/auth.json."
      }
    }
  },
  "uiHints": {
    "serverUrl": {
      "label": "Server URL",
      "placeholder": "http://localhost:3000"
    },
    "apiKey": {
      "label": "API Key or Token",
      "placeholder": "crmy_... or login token"
    }
  }
}

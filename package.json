{
  "name": "@crmy/core",
  "version": "0.9.5",
  "description": "CRMy — operational customer context for AI agents",
  "private": false,
  "files": [
    "README.md",
    "CHANGELOG.md",
    "RELEASE_NOTES.md",
    "LICENSE",
    "NOTICE"
  ],
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/cli",
    "packages/web",
    "packages/openclaw-plugin"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/web && npm run build --workspace=packages/server && npm run build --workspace=packages/cli && npm run build --workspace=packages/openclaw-plugin",
    "dev": "npm run dev:server & npm run dev:web & wait",
    "dev:server": "npm run dev --workspace=packages/server",
    "dev:web": "npm run dev --workspace=packages/web",
    "test": "node --test packages/server/test/durability.test.mjs",
    "test:cli-coverage": "node --test packages/cli/test/coverage.test.mjs",
    "test:ui-smoke": "node scripts/ui-smoke.mjs",
    "lint": "tsc -p packages/shared/tsconfig.json --noEmit && tsc -p packages/server/tsconfig.json --noEmit && tsc -p packages/cli/tsconfig.json --noEmit && tsc -p packages/web/tsconfig.json --noEmit",
    "migrate": "tsx scripts/migrate.ts",
    "start:server": "node packages/server/dist/index.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
